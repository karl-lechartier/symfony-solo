{% extends 'base.html.twig' %}

{% block title %}Supprimer une catégorie{% endblock %}

{% block body %}
    <h1>Supprimer le propriétaire <i>{{ proprietaire.nom }} {{ proprietaire.nom }}?</i></h1>
    {{ form(formulaire) }}
    {#je vais mettre un petit bouton retour...#}
    <a href="{{ path("app_proprietaires_voir") }}" class="btn btn-default">Retour</a>
{% endblock %}
